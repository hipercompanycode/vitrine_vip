# Plano Geo — Cidade + Cidades Próximas — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development. Steps use checkbox (`- [ ]`) syntax.

**Goal:** Visitante filtra a home por cidade e por cidades próximas (≤100km), detectando por GPS ou escolhendo manualmente; dataset IBGE completo, cálculo em SQL, grátis.

**Architecture:** Script local baixa municípios IBGE (lat/lng) e gera seed. Migration 0006 adiciona `unique(name,uf)` + funções `nearby_city_ids`/`nearest_city_id` + índice. Rotas de servidor setam cookies (`city_id`, `nearby`) e resolvem GPS→cidade. `HomeFilters` (client) vira funcional; a home lê cookies e filtra os anúncios.

**Tech Stack:** Next.js 16, Tailwind v4, Supabase (Postgres RPC), Vitest.

## Global Constraints
- UI pt-BR. Nível **cidade** (sem mapa/API paga). Raio **100km**.
- Cookie `city_id` (id) + `nearby` ("1"/"0", padrão "1"). Sem `city_id` → home mostra **todos** os anúncios visíveis.
- Re-seed de `cities` **não pode quebrar FK** (profiles/ads referenciam cities): usar `unique(name,uf)` + `on conflict (name,uf) do nothing` (mantém ids existentes, ex. São Paulo id=1).
- Aplicar no Supabase: primeiro **0006** (cria o unique), depois `cities_full.sql`.
- Reusa `lib/geo.ts` (haversine, nearestCity) já testado.

## File Structure
```
scripts/gen-cities.mjs                 # baixa IBGE + gera seed (dados fora do chat)
supabase/seed/cities_full.sql          # gerado (~5570 cidades, on conflict do nothing)
supabase/migrations/0006_geo.sql       # unique(name,uf) + funcoes + indice
src/app/api/geo/cities/route.ts        # GET ?q= busca cidades
src/app/api/geo/select/route.ts        # POST seta cookie city_id + nearby, redirect /
src/app/api/geo/nearest/route.ts       # GET ?lat&lng → nearest city + cookie
src/components/HomeFilters.tsx (modify) # seletor + GPS + toggle (client, real)
src/app/page.tsx (modify)              # lê cookies, filtra ads por cidade/proximas
```

---

### Task 1: Script gen-cities + seed

**Files:** Create `scripts/gen-cities.mjs`; gera `supabase/seed/cities_full.sql`.

- [ ] **Step 1: Script** — Create `scripts/gen-cities.mjs`:
```js
// Baixa municípios IBGE (kelvins/municipios-brasileiros) e gera seed SQL.
// Uso: node scripts/gen-cities.mjs   (os dados não passam pelo chat)
import { writeFileSync } from "node:fs";

const MUN = "https://raw.githubusercontent.com/kelvins/municipios-brasileiros/main/csv/municipios.csv";
const EST = "https://raw.githubusercontent.com/kelvins/municipios-brasileiros/main/csv/estados.csv";

function parseCsv(text) {
  const [head, ...rows] = text.trim().split(/\r?\n/);
  const cols = head.split(",");
  return rows.filter(Boolean).map((r) => {
    const v = r.split(",");
    return Object.fromEntries(cols.map((c, i) => [c.trim(), (v[i] ?? "").trim()]));
  });
}

const est = parseCsv(await (await fetch(EST)).text());   // codigo_uf,uf,nome,...
const ufByCode = Object.fromEntries(est.map((e) => [e.codigo_uf, e.uf]));

const mun = parseCsv(await (await fetch(MUN)).text());    // codigo_ibge,nome,latitude,longitude,capital,codigo_uf,...
const vals = mun
  .filter((m) => m.nome && m.latitude && m.longitude && ufByCode[m.codigo_uf])
  .map((m) => `('${m.nome.replace(/'/g, "''")}','${ufByCode[m.codigo_uf]}',${Number(m.latitude)},${Number(m.longitude)})`);

const sql =
  "-- IBGE municípios (gerado por scripts/gen-cities.mjs). Aplicar DEPOIS da migration 0006.\n" +
  "insert into public.cities (name, uf, lat, lng) values\n" +
  vals.join(",\n") +
  "\non conflict (name, uf) do nothing;\n";

writeFileSync("supabase/seed/cities_full.sql", sql);
console.log("cidades geradas:", vals.length);
```

- [ ] **Step 2: Rodar e verificar**

Run: `node scripts/gen-cities.mjs`
Expected: imprime `cidades geradas: ` com número ~5000–5600; cria `supabase/seed/cities_full.sql`.
Run: `wc -l supabase/seed/cities_full.sql` → milhares de linhas.
Se o fetch falhar (rede), reportar BLOCKED com o erro.

- [ ] **Step 3: Commit** (o seed gerado é grande mas versionado)

```bash
git add scripts/gen-cities.mjs supabase/seed/cities_full.sql
git commit -m "feat(geo): script IBGE + seed completo de cidades"
```

---

### Task 2: Migration 0006 (unique + funções + índice)

**Files:** Create `supabase/migrations/0006_geo.sql`

- [ ] **Step 1: Migration** — Create `supabase/migrations/0006_geo.sql`:
```sql
-- Unique para permitir re-seed idempotente sem quebrar FK
alter table public.cities add constraint if not exists cities_name_uf_uniq unique (name, uf);
create index if not exists cities_latlng_idx on public.cities (lat, lng);

-- Cidade mais próxima de um ponto (ordena pelo termo haversine, monotônico com a distância)
create or replace function public.nearest_city_id(p_lat double precision, p_lng double precision)
returns int language sql stable as $$
  select id from public.cities
  order by (
    power(sin(radians(lat - p_lat) / 2), 2) +
    cos(radians(p_lat)) * cos(radians(lat)) * power(sin(radians(lng - p_lng) / 2), 2)
  )
  limit 1;
$$;

-- Ids das cidades a ≤ p_km de p_city_id (inclui a própria); bounding-box + haversine
create or replace function public.nearby_city_ids(p_city_id int, p_km double precision)
returns setof int language sql stable as $$
  with c as (select lat, lng from public.cities where id = p_city_id)
  select ci.id
  from public.cities ci, c
  where ci.lat between c.lat - (p_km / 111.0) and c.lat + (p_km / 111.0)
    and ci.lng between c.lng - (p_km / (111.0 * cos(radians(c.lat)))) and c.lng + (p_km / (111.0 * cos(radians(c.lat))))
    and 2 * 6371 * asin(sqrt(
      power(sin(radians(ci.lat - c.lat) / 2), 2) +
      cos(radians(c.lat)) * cos(radians(ci.lat)) * power(sin(radians(ci.lng - c.lng) / 2), 2)
    )) <= p_km;
$$;
```
> Nota: `add constraint if not exists` não existe em todo Postgres; se der erro na aplicação, trocar por bloco `do $$ begin ... exception when duplicate_object then null; end $$;` ao aplicar. (O implementador só autora o SQL; a aplicação é na config.)

- [ ] **Step 2: Commit**

```bash
git add supabase/migrations/0006_geo.sql
git commit -m "feat(db): migration 0006 - unique cities + funcoes geo"
```

---

### Task 3: Rotas geo (cities, select, nearest)

**Files:** Create `src/app/api/geo/cities/route.ts`, `src/app/api/geo/select/route.ts`, `src/app/api/geo/nearest/route.ts`

- [ ] **Step 1: Busca de cidades** — Create `src/app/api/geo/cities/route.ts`:
```ts
import { NextResponse } from "next/server";
import { createAdminClient } from "@/lib/supabase/server";

export async function GET(request: Request) {
  const q = new URL(request.url).searchParams.get("q")?.trim() ?? "";
  if (q.length < 2) return NextResponse.json({ cities: [] });
  const admin = createAdminClient();
  const { data } = await admin
    .from("cities").select("id, name, uf").ilike("name", `${q}%`).order("name").limit(15);
  return NextResponse.json({ cities: data ?? [] });
}
```

- [ ] **Step 2: Selecionar cidade** — Create `src/app/api/geo/select/route.ts`:
```ts
import { NextResponse } from "next/server";

export async function POST(request: Request) {
  const form = await request.formData();
  const cityId = String(form.get("city_id") ?? "");
  const nearby = String(form.get("nearby") ?? "1") === "0" ? "0" : "1";

  const res = NextResponse.redirect(new URL("/", request.url), { status: 303 });
  if (cityId === "" || cityId === "0") {
    res.cookies.set("city_id", "", { maxAge: 0, path: "/" });
  } else {
    res.cookies.set("city_id", cityId, { path: "/", maxAge: 60 * 60 * 24 * 180 });
  }
  res.cookies.set("nearby", nearby, { path: "/", maxAge: 60 * 60 * 24 * 180 });
  return res;
}
```

- [ ] **Step 3: Cidade mais próxima (GPS)** — Create `src/app/api/geo/nearest/route.ts`:
```ts
import { NextResponse } from "next/server";
import { createAdminClient } from "@/lib/supabase/server";

export async function GET(request: Request) {
  const sp = new URL(request.url).searchParams;
  const lat = Number(sp.get("lat"));
  const lng = Number(sp.get("lng"));
  if (!Number.isFinite(lat) || !Number.isFinite(lng)) {
    return NextResponse.json({ error: "lat/lng inválidos" }, { status: 400 });
  }
  const admin = createAdminClient();
  const { data: id } = await admin.rpc("nearest_city_id", { p_lat: lat, p_lng: lng });
  if (id == null) return NextResponse.json({ error: "cidade não encontrada" }, { status: 404 });
  const { data: city } = await admin.from("cities").select("id, name, uf").eq("id", id).maybeSingle();

  const res = NextResponse.json({ city });
  res.cookies.set("city_id", String(id), { path: "/", maxAge: 60 * 60 * 24 * 180 });
  return res;
}
```

- [ ] **Step 4: Build check** — `npm run build` → ok.
- [ ] **Step 5: Commit**

```bash
git add src/app/api/geo
git commit -m "feat(geo): rotas de busca de cidade, selecao e nearest (GPS)"
```

---

### Task 4: HomeFilters funcional (client)

**Files:** Modify `src/components/HomeFilters.tsx`

- [ ] **Step 1: Reescrever** — Replace `src/components/HomeFilters.tsx`:
```tsx
"use client";
import { useState } from "react";

type City = { id: number; name: string; uf: string };

export default function HomeFilters({
  cityLabel, nearby,
}: { cityLabel: string; nearby: boolean }) {
  const [q, setQ] = useState("");
  const [opts, setOpts] = useState<City[]>([]);
  const [busy, setBusy] = useState(false);

  async function search(v: string) {
    setQ(v);
    if (v.trim().length < 2) { setOpts([]); return; }
    const res = await fetch(`/api/geo/cities?q=${encodeURIComponent(v)}`);
    const j = await res.json();
    setOpts(j.cities ?? []);
  }

  async function pick(cityId: string | number) {
    setBusy(true);
    const body = new FormData();
    body.set("city_id", String(cityId));
    body.set("nearby", nearby ? "1" : "0");
    await fetch("/api/geo/select", { method: "POST", body });
    window.location.href = "/";
  }

  async function toggleNearby() {
    setBusy(true);
    const body = new FormData();
    body.set("nearby", nearby ? "0" : "1");
    body.set("keep", "1"); // não altera a cidade, só o toggle "próximas"
    await fetch("/api/geo/select", { method: "POST", body });
    window.location.href = "/";
  }

  function useGPS() {
    if (!navigator.geolocation) return;
    setBusy(true);
    navigator.geolocation.getCurrentPosition(
      async (pos) => {
        await fetch(`/api/geo/nearest?lat=${pos.coords.latitude}&lng=${pos.coords.longitude}`);
        window.location.href = "/";
      },
      () => setBusy(false),
      { timeout: 8000 }
    );
  }

  return (
    <div className="rounded-card border border-line bg-surface/70 p-2 shadow-card backdrop-blur">
      <div className="flex flex-col gap-2 sm:flex-row sm:items-center">
        <div className="flex items-center gap-2 rounded-input px-2 text-sm text-ink">
          <svg width="16" height="16" viewBox="0 0 24 24" fill="none" aria-hidden="true">
            <path d="M12 21s-6-5.2-6-10a6 6 0 1 1 12 0c0 4.8-6 10-6 10z" stroke="currentColor" strokeWidth="1.8" />
            <circle cx="12" cy="11" r="2.2" fill="currentColor" />
          </svg>
          <span className="font-semibold">{cityLabel}</span>
        </div>
        <div className="hidden h-6 w-px bg-line sm:block" />
        <div className="relative flex-1">
          <input value={q} onChange={(e) => search(e.target.value)} placeholder="Trocar cidade…"
            disabled={busy}
            className="w-full rounded-input bg-transparent px-2 py-2 text-sm text-ink placeholder:text-muted focus:outline-none" />
          {opts.length > 0 && (
            <ul className="absolute z-30 mt-1 max-h-60 w-full overflow-auto rounded-input border border-line bg-surface shadow-pop">
              {opts.map((c) => (
                <li key={c.id}>
                  <button onClick={() => pick(c.id)} className="block w-full px-3 py-2 text-left text-sm hover:bg-accent-soft">
                    {c.name} - {c.uf}
                  </button>
                </li>
              ))}
            </ul>
          )}
        </div>
        <button onClick={useGPS} disabled={busy}
          className="rounded-input border border-line px-3 py-2 text-sm font-semibold text-ink hover:bg-accent-soft">
          Usar minha localização
        </button>
      </div>
      <div className="mt-2 flex items-center gap-2 px-2">
        <button onClick={toggleNearby} disabled={busy}
          className={`rounded-pill px-3 py-1 text-xs font-semibold transition-colors ${nearby ? "bg-accent text-white" : "border border-line text-muted"}`}>
          {nearby ? "Incluindo cidades próximas (100km)" : "Só esta cidade"}
        </button>
      </div>
    </div>
  );
}
```
> A rota `/api/geo/select` precisa suportar o campo `keep`: quando `keep=1`, NÃO altera `city_id` (só o `nearby`). Ajuste a rota select (Task 3 Step 2) adicionando no início do handler:
```ts
  const keep = String(form.get("keep") ?? "") === "1";
```
e trocar a lógica do cookie city_id para: só mexer no `city_id` quando `!keep`.

- [ ] **Step 2: Ajustar /api/geo/select para `keep`** — Modify `src/app/api/geo/select/route.ts` conforme a nota acima:
```ts
import { NextResponse } from "next/server";

export async function POST(request: Request) {
  const form = await request.formData();
  const keep = String(form.get("keep") ?? "") === "1";
  const cityId = String(form.get("city_id") ?? "");
  const nearby = String(form.get("nearby") ?? "1") === "0" ? "0" : "1";

  const res = NextResponse.redirect(new URL("/", request.url), { status: 303 });
  if (!keep) {
    if (cityId === "" || cityId === "0") res.cookies.set("city_id", "", { maxAge: 0, path: "/" });
    else res.cookies.set("city_id", cityId, { path: "/", maxAge: 60 * 60 * 24 * 180 });
  }
  res.cookies.set("nearby", nearby, { path: "/", maxAge: 60 * 60 * 24 * 180 });
  return res;
}
```

- [ ] **Step 3: Build check** — `npm run build` → ok.
- [ ] **Step 4: Commit**

```bash
git add src/components/HomeFilters.tsx src/app/api/geo/select/route.ts
git commit -m "feat(geo): HomeFilters funcional (seletor, GPS, toggle proximas)"
```

---

### Task 5: Home filtra por cidade/próximas

**Files:** Modify `src/app/page.tsx`

- [ ] **Step 1: Ler cookies + filtrar** — Modify `src/app/page.tsx`:

(a) Import cookies:
```tsx
import { cookies } from "next/headers";
```

(b) No topo do `Home()`, antes de montar a query de ads, resolver a cidade e o conjunto:
```tsx
const jar = await cookies();
const cityId = jar.get("city_id")?.value ? Number(jar.get("city_id")!.value) : null;
const nearby = (jar.get("nearby")?.value ?? "1") !== "0";

let cityFilter: number[] | null = null;
let cityLabel = "Todas as cidades";
if (cityId) {
  const { data: cityRow } = await admin.from("cities").select("name, uf").eq("id", cityId).maybeSingle();
  if (cityRow) cityLabel = `${cityRow.name} - ${cityRow.uf}`;
  if (nearby) {
    const { data: ids } = await admin.rpc("nearby_city_ids", { p_city_id: cityId, p_km: 100 });
    cityFilter = (ids ?? []).map((r: any) => (typeof r === "number" ? r : r.nearby_city_ids ?? r.id));
  } else {
    cityFilter = [cityId];
  }
}
```

(c) Na query de `ads` (segunda etapa), adicionar o filtro de cidade quando houver:
```tsx
    let query = admin
      .from("ads")
      .select(`
        id, title, description, price_cents, is_available, created_at,
        cities ( name, uf ),
        profiles ( whatsapp )
      `)
      .eq("status", "active")
      .in("profile_id", activeProfileIds);
    if (cityFilter) query = query.in("city_id", cityFilter);
    const res = await query
      .order("bumped_at", { ascending: false, nullsFirst: false })
      .order("created_at", { ascending: false });
```

(d) Passar props ao `HomeFilters`:
```tsx
<HomeFilters cityLabel={cityLabel} nearby={nearby} />
```

- [ ] **Step 2: Build + suíte** — `npm run build` → ok. `npm test` → PASS (mesmos testes).

- [ ] **Step 3: Commit**

```bash
git add src/app/page.tsx
git commit -m "feat(geo): home filtra por cidade + cidades proximas (cookies)"
```

---

### Task 6: Preview + verificação

**Files:** Modify `src/app/preview/page.tsx`

- [ ] **Step 1: Preview** — `HomeFilters` agora exige props `cityLabel`/`nearby`. No `src/app/preview/page.tsx`, passar valores estáticos: `<HomeFilters cityLabel="São Paulo - SP" nearby={true} />`. (No preview os botões chamam as rotas reais; é só visual — sem cookie real muda pouco.)

- [ ] **Step 2: Build + suíte** — `npm run build` → ok. `npm test` → PASS.

- [ ] **Step 3: Commit**

```bash
git add src/app/preview/page.tsx
git commit -m "chore(preview): HomeFilters com props"
```

---

## Self-Review (cobertura da spec)
- Dataset IBGE completo via script local: Task 1 ✔
- Função nearby (bounding-box + haversine) + nearest + unique/índice: Task 2 ✔
- Rotas cities/select/nearest + cookies: Tasks 3,4 ✔
- HomeFilters funcional (seletor, GPS, toggle): Task 4 ✔
- Home filtra por cidade/próximas: Task 5 ✔
- Aplicar no Supabase (0006 antes do seed): fase de config (após tasks).

## Placeholders
Nenhum. A rota select tem 2 versões no doc (Task 3 inicial e Task 4 Step 2 com `keep`) — a de Task 4 é a final; o implementador aplica a versão com `keep`.
